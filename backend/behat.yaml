default:
  suites:
    create:
      paths:
        - "%paths.base%/features/"
      contexts: [CreateContext]

    query:
      paths:
        - "%paths.base%/features/query"
      contexts: [QueryContext]